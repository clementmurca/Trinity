import { View } from 'react-native';
import CartScreen from '../cart/cart';
export default function AccountScreen() {
    return (
        <View style={{ flex: 1 }}>
            <CartScreen />
        </View>
    );
}